/**
 * Controladores de la interfaz web MVC.
 *
 * <p>{@code @Controller} registra una clase que devuelve nombres de vistas Thymeleaf.
 * {@code @GetMapping} y {@code @PostMapping} conectan métodos con solicitudes HTTP, {@code Model}
 * transporta datos a las vistas, {@code @ModelAttribute} vincula formularios a objetos Java y
 * {@code @PathVariable} obtiene partes variables de las URL.
 */
package ar.edu.is2.ejercicio4.controller;
